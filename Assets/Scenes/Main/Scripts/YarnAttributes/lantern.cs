using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Daydream
{
    public class lantern : MonoBehaviour
    {
        [YarnCommand("lantern")]
        public void LanternStart()
        {
            //create lantern
        }
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
